pipeline {
    agent any
    
    tools {
        maven 'maven' 
    }
    
    environment {
        DEPLOY_CREDENTIALS = 'ssh_deploy_server' 
        TARGET_HOST = '127.0.0.1' 
        TARGET_USER = 'ELITEBOOK' 
        TARGET_DIR = 'C:/Temp' 
        // ******************************************************
        // CORRECTION MAJEURE: Le nom du JAR doit correspondre au nom du projet Maven
        // ******************************************************
        JAR_FILE = 'Point-of-sale-0.0.1-SNAPSHOT.jar'
    }

    stages {
        stage('Checkout') {
            steps {
                echo 'Cloning repository from GitHub...'
            }
        }
        stage('Build and Test') {
            steps {
                echo 'Building the application and running tests with Maven...'
                dir('POV-JAVA') {
                    bat 'mvn clean install' 
                }
            }
        }
        stage('Deploy') { 
            steps {
                echo "Copying application to ${TARGET_USER}@${TARGET_HOST}:${TARGET_DIR}"
                
                // Le chemin d'accès au JAR est maintenant correct :
                // POV-JAVA/target/Point-of-sale-0.0.1-SNAPSHOT.jar
                sshagent(credentials: [DEPLOY_CREDENTIALS]) {
                    // La commande SCP est maintenant correcte avec le bon nom de fichier
                    bat "scp POV-JAVA/target/${JAR_FILE} ${TARGET_USER}@${TARGET_HOST}:${TARGET_DIR}"
                }
                
                echo 'Deployment copy finished.'
            }
        }
    }
}